const nums = [-1, 2, 100, 10, 50]

const maxNumber = Math.max(...nums)
const minNumber = Math.min(...nums)

console.log('maxNumber  :::->>>', maxNumber); // 100
console.log('minNumber  :::->>>', minNumber);  //-1